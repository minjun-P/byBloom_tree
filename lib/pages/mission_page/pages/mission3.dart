import 'package:flutter/material.dart';

/// 일일미션3 - 교회 연계
class Mission3 extends StatelessWidget {
  const Mission3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('3'),
    );
  }
}
