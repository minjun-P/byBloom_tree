import 'package:flutter/material.dart';

class MissionBPage extends StatelessWidget {
  const MissionBPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('B'),
    );
  }
}
